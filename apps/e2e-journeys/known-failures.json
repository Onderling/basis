{
  "J-circle (join → noticeboard → private chat)": {
    "the removed member receives nothing the circle posts afterwards": "F-022 / ledger L44 — the fan-out peer set is reconciled against the roster at circle-open; this composition has no webid↔address mapping at all, so it cannot demonstrate the fix"
  }
}
