#!/usr/bin/env bash
# assistant role health: the container is running. The device holds an open relay socket and, with a
# token, long-polls Telegram; a bad token or an unreachable relay crashes the boot, which shows as
# not-running after the health timeout.
set -u
eval "$COMPOSE ps --status running --services" | grep -qx assistant
