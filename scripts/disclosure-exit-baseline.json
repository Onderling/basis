{
  "sites": [
    "apps/basis-mobile/src/screens/v2/CircleLauncherScreen.js • onReportMember={(m) => { const ref = m?.webid || m?.id; if (ref) fileCircleReportMobile('member', ref, m?.handle || m?.realName || ref); }}",
    "apps/basis-mobile/src/screens/v2/CircleMemberCardScreen.js • .map((m) => ({ id: m.id, kind: 'member', label: m.handle ? `@${m.handle}` : (m.realName || m.id) })),",
    "apps/basis-mobile/src/screens/v2/CircleMemberCardScreen.js • : ((realNameVisible && member.realName) ? member.realName : (member.handle ? `@${member.handle}` : (member.id || '')));",
    "apps/basis/src/v2/governanceView.js • const name = m.realName || m.displayName || m.handle || null;",
    "apps/basis/web/v2/circleApp.js • fileCircleReport(id, 'member', ref, m?.handle || m?.realName || ref, reason).then(() => showDetail(id)).catch(() => {});",
    "apps/basis/web/v2/circleMemberCard.js • .map((m) => ({ id: m.id, kind: 'member', label: m.handle ? `@${m.handle}` : (m.realName || m.id) })),",
    "apps/basis/web/v2/circleMemberCard.js • ? member.realName",
    "apps/basis/web/v2/circleMemberCard.js • title.textContent = (realNameVisible && member.realName)"
  ]
}
